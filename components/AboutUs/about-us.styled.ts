import styled from 'styled-components';

import { COMPONENT_SIZING } from '@constants/component-sizing.constant';

export const StyledAboutUs = styled.div`
  padding-top: ${COMPONENT_SIZING.header}rem;
`;
